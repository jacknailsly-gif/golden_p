diff --git a/lib/services/prediction_pipeline_service.dart b/lib/services/prediction_pipeline_service.dart
--- a/lib/services/prediction_pipeline_service.dart
+++ b/lib/services/prediction_pipeline_service.dart
@@ -235,1 +235,1 @@
-    if (context.incorrectStreak >= 2) {
+    if (context.incorrectStreak == 2) {
diff --git a/lib/viewmodels/sequence_analyzer_viewmodel.dart b/lib/viewmodels/sequence_analyzer_viewmodel.dart
--- a/lib/viewmodels/sequence_analyzer_viewmodel.dart
+++ b/lib/viewmodels/sequence_analyzer_viewmodel.dart
@@ -1819,12 +1819,25 @@
-    final PredictionResult result = _predictionPipeline.generateHybridResponse(
-      predictionContext,
-      _baseRateScores,
-      _recentSuccessScores,
-      _patternAvoidanceScores,
-      _counterMoveMemory,
-      _decisionSequence,
-    );
-
-    _primaryPrediction = result.primaryPrediction;
-    _decisionSource = result.decisionSource;
+    if (_predictionMode == 'copy_user' && _inputs.isNotEmpty) {
+      _primaryPrediction = _inputs.last.selectedPos ?? _inputs.last.value;
+      _decisionSource = 'Copy User (Mode)';
+    } else {
+      final PredictionResult result = _predictionPipeline.generateHybridResponse(
+        predictionContext,
+        _baseRateScores,
+        _recentSuccessScores,
+        _patternAvoidanceScores,
+        _counterMoveMemory,
+        _decisionSequence,
+      );
+      _primaryPrediction = result.primaryPrediction;
+      _decisionSource = result.decisionSource;
+    }
diff --git a/lib/viewmodels/overlay_buttons_viewmodel.dart b/lib/viewmodels/overlay_buttons_viewmodel.dart
--- a/lib/viewmodels/overlay_buttons_viewmodel.dart
+++ b/lib/viewmodels/overlay_buttons_viewmodel.dart
@@ -1148,1 +1148,1 @@
-          } else if (_consecutiveRecoveryLosses >= 5 && _recoveryMode != 1) {
+          } else if (_consecutiveRecoveryLosses >= 5) {
@@ -1175,22 +1175,27 @@
           if (!isSafetyValveActive) {
-            if (_consecutiveLossesStreak == 2 && _recoveryMode != 1) {
-               // 2-Loss Soft Brake Check
-               double currentConfidence = _sequenceAnalyzerViewModel.confidence;
-               bool isSafe = MacroRiskManager.instance.isSafeToRecover();
-               
-               if (currentConfidence < 0.6 || !isSafe) {
-                  debugPrint('[V41.0 🛑 SOFT BRAKE] 2 consecutive losses + low confidence/high hostility. Halting recovery to probe graph.');
-                  _isRecoveryUnlocked = false;
-                  _recoveryWinsRequired = 1; // Need 1 win to prove safety before recovering
-                  await _ensureBaseBet(runToken);
-               } else {
-                  debugPrint('[V41.0] ⚡ Confidence is high. Proceeding with 3rd escalation bet.');
-                  _recoveryWinsRequired = 0;
-                  _isRecoveryUnlocked = true;
-               }
-            } else {
-               debugPrint('[V22.0] ⚡ Immediate Recovery Triggered after loss.');
-               _recoveryWinsRequired = 0;
-               _isRecoveryUnlocked = true;
-            }
+            if (_isTrapBreakerActive) {
+               debugPrint('[V64.0 TRAP BREAKER] 🛡️ Lost during Trap Breaker. Keeping recovery locked and bet at base.');
+               _isRecoveryUnlocked = false;
+               _recoveryWinsRequired = 999;
+               await _ensureBaseBet(runToken);
+            } else if (_consecutiveLossesStreak == 2 && _recoveryMode != 1) {
+               // 2-Loss Soft Brake Check
+               double currentConfidence = _sequenceAnalyzerViewModel.confidence;
+               bool isSafe = MacroRiskManager.instance.isSafeToRecover();
+               
+               if (currentConfidence < 0.6 || !isSafe) {
+                  debugPrint('[V41.0 🛑 SOFT BRAKE] 2 consecutive losses + low confidence/high hostility. Halting recovery to probe graph.');
+                  _isRecoveryUnlocked = false;
+                  _recoveryWinsRequired = 1; // Need 1 win to prove safety before recovering
+                  await _ensureBaseBet(runToken);
+               } else {
+                  debugPrint('[V41.0] ⚡ Confidence is high. Proceeding with 3rd escalation bet.');
+                  _recoveryWinsRequired = 0;
+                  _isRecoveryUnlocked = true;
+               }
+            } else {
+               debugPrint('[V22.0] ⚡ Immediate Recovery Triggered after loss.');
+               _recoveryWinsRequired = 0;
+               _isRecoveryUnlocked = true;
+            }
           }
@@ -1204,1 +1209,1 @@
-          if (_isRecoveryUnlocked) {
+          if (_isRecoveryUnlocked && !_isTrapBreakerActive) {
